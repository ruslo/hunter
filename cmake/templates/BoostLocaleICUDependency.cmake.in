# Copyright (c) 2015, 2019, Ruslan Baratov
# All rights reserved.

# Do not call 'hunter_add_package(ICU)' because ICU it not a basic dependency
find_package(ICU CONFIG REQUIRED)

get_target_property(
   _interface_link_libraries Boost::locale INTERFACE_LINK_LIBRARIES
)

set_target_properties(
    Boost::locale
    PROPERTIES
    INTERFACE_LINK_LIBRARIES "${_interface_link_libraries};ICU::i18n"
)
